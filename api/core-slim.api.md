# core/slim: public API surface

Auto-generated by `scripts/api-snapshot.mjs`. Do not edit by hand.
Regenerate via `pnpm api:snapshot` after a deliberate API change.

## Exports (235)

### interface `Animatable`

```ts
export interface Animatable {
    animate(keyframes: Keyframe[], options: {
        duration: number;
        easing: string;
        fill?: "forwards" | "none" | "both";
    }): WaapiAnimation;
}
```

### interface `AnimationDef`

```ts
export interface AnimationDef<T> {
    readonly interpolate: Interpolator<T>;
    readonly duration: number;
    readonly easing?: EasingFn;
    readonly linearizable?: boolean;
    readonly properties?: readonly string[];
    readonly tierSplit?: {
        readonly props: readonly string[];
        readonly compositor: readonly string[];
        readonly main: readonly string[];
    };
    readonly commit?: (p: number, el: CommitTarget) => void;
}
```

### type `AnimationProps`

```ts
export type AnimationProps = Readonly<Record<string, PropertyValue>>;
```

### interface `AnimationRecord`

```ts
export interface AnimationRecord {
    readonly id: number;
    readonly duration: number;
    readonly targets: readonly StrategyTarget[];
    readonly startedAt: number;
    readonly state: StrategyState;
    readonly backend: StrategyBackend;
    readonly progress: number;
    readonly controls: Controls;
}
```

### interface `Batch`

```ts
export interface Batch {
    read<T>(fn: ReadJob<T>): Promise<T>;
    write(fn: WriteJob): Promise<void>;
    flush(): {
        reads: number;
        writes: number;
    };
    readonly size: number;
}
```

### interface `Clock`

```ts
export interface Clock {
    now(): number;
    nowAt(realTime: number): number;
    pause(): void;
    resume(): void;
    readonly paused: boolean;
    setSpeed(multiplier: number): void;
    readonly speed: number;
    reset(): void;
}
```

### interface `ClockOpts`

```ts
export interface ClockOpts {
    readonly now?: NowFn;
    readonly speed?: number;
}
```

### type `ColorFormat`

```ts
export type ColorFormat = "hex" | "rgb" | "hsl" | "oklch";
```

### interface `Computer`

```ts
export interface Computer {
    compute(specs: readonly WorkerAnimSpec[], time: number): Promise<WorkerValues>;
    computeSync(specs: readonly WorkerAnimSpec[], time: number): WorkerValues;
    terminate(): void;
    readonly mode: "inline" | "worker";
}
```

### type `ComputerMode`

```ts
export type ComputerMode = "auto" | "inline" | "worker";
```

### interface `Controls`

```ts
export interface Controls extends PromiseLike<void> {
    pause(): Controls;
    resume(): Controls;
    seek(progress: number): Controls;
    seekLabel(label: string): Controls;
    reverse(): Controls;
    restart(): Controls;
    cancel(): Controls;
    readonly state: StrategyState;
    readonly duration: number;
    readonly progress: number;
    readonly direction: 1 | -1;
    readonly finished: Promise<void>;
    readonly labels: ReadonlyMap<string, number>;
    speed: number;
    catch<R>(onrejected: (err: unknown) => R | PromiseLike<R>): Promise<void | R>;
    finally(onfinally?: (() => void) | null): Promise<void>;
}
```

### type `DragAxis`

```ts
export type DragAxis = "x" | "y" | "both";
```

### interface `DragBounds`

```ts
export interface DragBounds {
    readonly left?: number;
    readonly right?: number;
    readonly top?: number;
    readonly bottom?: number;
}
```

### interface `DragEvent`

```ts
export interface DragEvent {
    readonly offset: Point;
    readonly velocity: Point;
    readonly pointerEvent: PointerEventShim;
}
```

### interface `DragHandle`

```ts
export interface DragHandle {
    cancel(): void;
    readonly phase: DragPhase;
    readonly offset: Point;
    readonly velocity: Point;
}
```

### interface `DragOpts`

```ts
export interface DragOpts {
    readonly target: PointerBindTarget;
    readonly axis?: DragAxis;
    readonly bounds?: DragBounds;
    readonly snap?: DragSnap;
    readonly release?: DragReleaseOpts | false;
    readonly source?: PointerSource;
    readonly scheduler?: FrameScheduler;
    readonly clock?: Clock;
    readonly applyTouchAction?: boolean;
    readonly onStart?: (ev: DragEvent) => void;
    readonly onMove?: (ev: DragEvent) => void;
    readonly onEnd?: (ev: DragEvent) => void;
}
```

### type `DragPhase`

```ts
export type DragPhase = "idle" | "dragging" | "releasing" | "cancelled";
```

### type `DragPublicOpts`

```ts
export type DragPublicOpts = Omit<DragOpts, "target"> & {
    readonly target?: PointerBindTarget;
    readonly resolve?: PlayOpts["resolve"];
};
```

### interface `DragReleaseOpts`

```ts
export interface DragReleaseOpts {
    readonly duration?: number;
}
```

### interface `DragSnap`

```ts
export interface DragSnap {
    readonly points: readonly Point[];
    readonly threshold?: number;
}
```

### type `EasingFn`

```ts
export type EasingFn = (progress: number) => number;
```

### interface `ElementShim`

```ts
export interface ElementShim {
    readonly style: StyleShim;
    setAttribute(name: string, value: string): void;
}
```

### type `FrameJob`

```ts
export type FrameJob = (state: FrameState) => void;
```

### interface `FrameScheduler`

```ts
export interface FrameScheduler {
    schedule(phase: Phase, fn: FrameJob, opts?: {
        keepalive?: boolean;
    }): void;
    cancel(phase: Phase, fn: FrameJob): void;
    scheduleNode(phase: Phase, node: KeepaliveNode): void;
    cancelNode(node: KeepaliveNode): void;
    flushSync(time?: number): FrameState;
    readonly tick: number;
    readonly isRunning: boolean;
}
```

### interface `FrameSchedulerOpts`

```ts
export interface FrameSchedulerOpts {
    readonly raf?: RafLike;
    readonly now?: () => number;
}
```

### interface `FrameState`

```ts
export interface FrameState {
    readonly time: number;
    readonly delta: number;
    readonly tick: number;
}
```

### interface `GLLike`

```ts
export interface GLLike {
    uniform1f(location: UniformLocation, v: number): void;
    uniform2fv(location: UniformLocation, v: Float32Array | readonly number[]): void;
    uniform3fv(location: UniformLocation, v: Float32Array | readonly number[]): void;
    uniform4fv(location: UniformLocation, v: Float32Array | readonly number[]): void;
    uniformMatrix4fv(location: UniformLocation, transpose: boolean, v: Float32Array | readonly number[]): void;
}
```

### type `GestureTarget`

```ts
export type GestureTarget = Target;
```

### interface `GridOpts`

```ts
export interface GridOpts {
    readonly rows: number;
    readonly cols: number;
    readonly origin?: "center" | "start" | "end" | readonly [
        row: number,
        col: number
    ];
    readonly metric?: "euclidean" | "manhattan" | "chebyshev";
}
```

### interface `HoverHandle`

```ts
export interface HoverHandle {
    cancel(): void;
    readonly state: HoverState;
    readonly isHovering: boolean;
}
```

### interface `HoverOpts`

```ts
export interface HoverOpts extends StrategyOpts {
    readonly target: PointerBindTarget;
    readonly enter: AnimationDef<AnimationProps>;
    readonly leave?: AnimationDef<AnimationProps>;
}
```

### type `HoverPublicOpts`

```ts
export type HoverPublicOpts = Omit<HoverOpts, "target"> & {
    readonly target?: PointerBindTarget;
    readonly resolve?: PlayOpts["resolve"];
};
```

### type `HoverState`

```ts
export type HoverState = "idle" | "active" | "cancelled";
```

### interface `InertiaOpts`

```ts
export interface InertiaOpts {
    readonly timeConstant?: number;
    readonly power?: number;
    readonly restDelta?: number;
    readonly bounds?: Readonly<Record<string, readonly [
        number,
        number
    ]>>;
}
```

### type `InertiaProps`

```ts
export type InertiaProps = Readonly<Record<string, readonly [
    number,
    number
]>>;
```

### type `InertiaValue`

```ts
export type InertiaValue<P extends InertiaProps> = {
    [K in keyof P]: number;
};
```

### type `Interpolator`

```ts
export type Interpolator<T> = (progress: number) => T;
```

### interface `InterpolatorEntry`

```ts
export interface InterpolatorEntry {
    readonly name: string;
    readonly test: (value: unknown) => boolean;
    readonly interpolate: ValueInterpolator<unknown>;
}
```

### interface `Keyframe`

```ts
export interface Keyframe {
    offset?: number;
    transform?: string;
    [key: string]: string | number | undefined;
}
```

### type `KeyframeStops`

```ts
export type KeyframeStops = Record<string, readonly unknown[]>;
```

### interface `KeyframesOpts`

```ts
export interface KeyframesOpts {
    readonly duration?: number;
    readonly easing?: EasingFn;
    readonly offsets?: readonly number[];
}
```

### type `KeyframesValue`

```ts
export type KeyframesValue<P extends KeyframeStops> = {
    [K in keyof P]: P[K] extends readonly (infer V)[] ? V : never;
};
```

### interface `KinemDevtoolsHook`

```ts
export interface KinemDevtoolsHook {
    readonly version: 1;
    listActive(): readonly AnimationRecord[];
    subscribe(fn: TrackerListener): () => void;
}
```

### class `KinemError`

```ts
export declare class KinemError extends Error {
    readonly hint: string;
    constructor(message: string, hint?: string);
}
```

### interface `LoopOpts`

```ts
export interface LoopOpts {
    readonly count?: number;
    readonly yoyo?: boolean;
}
```

### type `NowFn`

```ts
export type NowFn = () => number;
```

### type `ParallelValues`

```ts
export type ParallelValues<T extends readonly AnimationDef<unknown>[]> = {
    readonly [K in keyof T]: T[K] extends AnimationDef<infer U> ? U : never;
};
```

### interface `PathCommand`

```ts
export interface PathCommand {
    readonly type: string;
    readonly params: readonly number[];
}
```

### type `Phase`

```ts
export type Phase = "read" | "compute" | "update" | "render";
```

### type `PlayMode`

```ts
export type PlayMode = "auto" | "compositor" | "main";
```

### interface `PlayOpts`

```ts
export interface PlayOpts extends StrategyOpts {
    readonly mode?: PlayMode;
    readonly resolve?: (selector: string) => readonly StrategyTarget[];
}
```

### interface `PlayStaggerOpts`

```ts
export interface PlayStaggerOpts extends PlayOpts {
    readonly each: number;
    readonly from?: StaggerFrom;
}
```

### interface `Point`

```ts
export interface Point {
    readonly x: number;
    readonly y: number;
}
```

### interface `PointerBindTarget`

```ts
export interface PointerBindTarget {
    setPointerCapture?(pointerId: number): void;
    releasePointerCapture?(pointerId: number): void;
    hasPointerCapture?(pointerId: number): boolean;
    addEventListener(type: string, cb: (ev: PointerEventShim) => void, opts?: AddEventListenerOptions | boolean): void;
    removeEventListener(type: string, cb: (ev: PointerEventShim) => void): void;
}
```

### interface `PointerEventShim`

```ts
export interface PointerEventShim {
    readonly pointerId: number;
    readonly pointerType: string;
    readonly clientX: number;
    readonly clientY: number;
    readonly timeStamp: number;
    preventDefault?(): void;
}
```

### interface `PointerHandlers`

```ts
export interface PointerHandlers {
    onStart?(ev: PointerEventShim): void;
    onMove?(ev: PointerEventShim): void;
    onEnd?(ev: PointerEventShim): void;
    onCancel?(ev: PointerEventShim): void;
}
```

### interface `PointerSample`

```ts
export interface PointerSample {
    readonly x: number;
    readonly y: number;
    readonly time: number;
}
```

### interface `PointerSource`

```ts
export interface PointerSource {
    bind(el: PointerBindTarget, handlers: PointerHandlers): PointerUnsubscribe;
}
```

### type `PointerUnsubscribe`

```ts
export type PointerUnsubscribe = () => void;
```

### interface `PropertyInfo`

```ts
export interface PropertyInfo {
    readonly tier: PropertyTier;
    readonly target: string;
    readonly apply: "style" | "attr" | "transform";
}
```

### type `PropertyMap`

```ts
export type PropertyMap = Readonly<Record<string, PropertyValue>>;
```

### type `PropertyTier`

```ts
export type PropertyTier = "compositor" | "main" | "pseudo";
```

### type `PropertyValue`

```ts
export type PropertyValue = string | number;
```

### type `RafHandle`

```ts
export type RafHandle = TimingHandle;
```

### interface `RafLike`

```ts
export interface RafLike {
    request(cb: (time: number) => void): number;
    cancel(id: number): void;
}
```

### type `RafOpts`

```ts
export type RafOpts = TimingOpts;
```

### type `RafState`

```ts
export type RafState = TimingHandle["state"];
```

### type `ReadJob`

```ts
export type ReadJob<T> = () => T;
```

### type `ReducedMotion`

```ts
export type ReducedMotion = "always" | "never" | "user";
```

### interface `ScrollBounds`

```ts
export interface ScrollBounds {
    readonly scrollStart: number;
    readonly scrollEnd: number;
}
```

### type `ScrollHandle`

```ts
export type ScrollHandle = ScrollSyncHandle | ScrollTriggeredHandle;
```

### interface `ScrollOpts`

```ts
export interface ScrollOpts extends Omit<ScrollTriggeredOpts, "source" | "start" | "end" | "toggleActions">, Omit<ScrollSyncOpts, "source" | "start" | "end"> {
    readonly sync?: boolean;
    readonly trigger?: ScrollTriggerConfig;
    readonly toggleActions?: string | ToggleActions;
    readonly source?: ScrollSource;
    readonly resolve?: PlayOpts["resolve"];
}
```

### interface `ScrollRect`

```ts
export interface ScrollRect {
    readonly top: number;
    readonly height: number;
}
```

### interface `ScrollSource`

```ts
export interface ScrollSource {
    getScrollY(): number;
    getViewportHeight(): number;
    getRect(el: StrategyTarget): ScrollRect;
    onScroll(cb: () => void): ScrollUnsubscribe;
    onResize(cb: () => void): ScrollUnsubscribe;
}
```

### interface `ScrollSyncHandle`

```ts
export interface ScrollSyncHandle {
    cancel(): void;
    readonly state: ScrollSyncState;
    readonly progress: number;
}
```

### interface `ScrollSyncOpts`

```ts
export interface ScrollSyncOpts {
    readonly source: ScrollSource;
    readonly start: TriggerPos;
    readonly end: TriggerPos;
    readonly scheduler?: FrameScheduler;
    readonly onProgress?: (progress: number) => void;
}
```

### type `ScrollSyncState`

```ts
export type ScrollSyncState = "active" | "cancelled";
```

### interface `ScrollTriggerConfig`

```ts
export interface ScrollTriggerConfig {
    readonly start?: string | TriggerPos;
    readonly end?: string | TriggerPos;
}
```

### interface `ScrollTriggeredHandle`

```ts
export interface ScrollTriggeredHandle {
    cancel(): void;
    readonly controls: Controls;
    readonly state: ScrollTriggeredState;
}
```

### interface `ScrollTriggeredOpts`

```ts
export interface ScrollTriggeredOpts extends StrategyOpts {
    readonly source: ScrollSource;
    readonly start: TriggerPos;
    readonly end: TriggerPos;
    readonly toggleActions?: string | ToggleActions;
    readonly scheduler?: FrameScheduler;
}
```

### type `ScrollTriggeredState`

```ts
export type ScrollTriggeredState = "idle" | "active" | "cancelled";
```

### type `ScrollUnsubscribe`

```ts
export type ScrollUnsubscribe = () => void;
```

### type `ScrollZone`

```ts
export type ScrollZone = "before" | "active" | "after";
```

### interface `ShuffleOpts`

```ts
export interface ShuffleOpts {
    readonly seed?: number;
}
```

### type `SplitBy`

```ts
export type SplitBy = "chars" | "words" | "lines";
```

### interface `SplitOpts`

```ts
export interface SplitOpts {
    readonly by?: SplitBy | readonly SplitBy[];
    readonly charClass?: string;
    readonly wordClass?: string;
    readonly lineClass?: string;
    readonly wrapperTag?: string;
}
```

### interface `SplitResult`

```ts
export interface SplitResult {
    readonly chars: readonly HTMLElement[];
    readonly words: readonly HTMLElement[];
    readonly lines: readonly HTMLElement[];
    revert(): void;
}
```

### type `SpringEasingFn`

```ts
export type SpringEasingFn = EasingFn & {
    readonly duration: number;
};
```

### interface `SpringOpts`

```ts
export interface SpringOpts {
    readonly stiffness?: number;
    readonly damping?: number;
    readonly mass?: number;
    readonly velocity?: number;
    readonly restVelocity?: number;
    readonly restDisplacement?: number;
    readonly maxDuration?: number;
}
```

### interface `StaggerArrayOpts`

```ts
export interface StaggerArrayOpts {
    readonly each: number;
    readonly from?: StaggerFrom;
}
```

### type `StaggerFn`

```ts
export type StaggerFn = (index: number, count: number) => number;
```

### type `StaggerFrom`

```ts
export type StaggerFrom = "start" | "end" | "center" | "edges" | number | StaggerFn;
```

### interface `StaggerOpts`

```ts
export interface StaggerOpts {
    readonly each: number;
    readonly count: number;
    readonly from?: StaggerFrom;
}
```

### type `StepPosition`

```ts
export type StepPosition = "start" | "end" | "jump-none" | "jump-both";
```

### type `StrategyBackend`

```ts
export type StrategyBackend = "auto" | "waapi" | "raf";
```

### interface `StrategyHandle`

```ts
export interface StrategyHandle {
    pause(): void;
    resume(): void;
    seek(progress: number): void;
    reverse(): void;
    setSpeed(multiplier: number): void;
    cancel(): void;
    readonly state: StrategyState;
    readonly progress: number;
    readonly direction: 1 | -1;
    readonly finished: Promise<void>;
}
```

### interface `StrategyOpts`

```ts
export interface StrategyOpts extends WaapiOpts, RafOpts {
    readonly backend?: StrategyBackend;
    readonly waapiSupported?: boolean;
    readonly lazy?: boolean;
    readonly reducedMotion?: ReducedMotion;
}
```

### type `StrategyState`

```ts
export type StrategyState = "idle" | "playing" | "paused" | "finished" | "cancelled";
```

### interface `StrategyTarget`

```ts
export interface StrategyTarget extends ElementShim, Animatable {
}
```

### interface `StrokeDrawOpts`

```ts
export interface StrokeDrawOpts {
    readonly pathLength: number;
    readonly duration?: number;
    readonly reverse?: boolean;
}
```

### interface `StyleShim`

```ts
export interface StyleShim {
    setProperty(name: string, value: string): void;
}
```

### type `Target`

```ts
export type Target = string | StrategyTarget | readonly StrategyTarget[] | ArrayLike<StrategyTarget>;
```

### interface `Timeline`

```ts
export interface Timeline {
    add(def: AnimationDef<AnimationProps>, target: Target, opts?: TimelineAddOpts): Timeline;
    addLabel(name: string, at?: TimelinePosition, offset?: number): Timeline;
    play(opts?: PlayOpts): Controls;
    readonly duration: number;
    readonly labels: ReadonlyMap<string, number>;
}
```

### interface `TimelineAddOpts`

```ts
export interface TimelineAddOpts {
    readonly at?: TimelinePosition;
    readonly offset?: number;
    readonly label?: string;
}
```

### type `TimelinePosition`

```ts
export type TimelinePosition = number | "<" | ">" | string;
```

### interface `TimingHandle`

```ts
export interface TimingHandle {
    pause(): void;
    resume(): void;
    seek(progress: number): void;
    reverse(): void;
    setSpeed(multiplier: number): void;
    cancel(): void;
    readonly state: TimingState;
    readonly progress: number;
    readonly direction: 1 | -1;
    readonly finished: Promise<void>;
}
```

### interface `TimingOpts`

```ts
export interface TimingOpts {
    readonly scheduler?: FrameScheduler;
    readonly clock?: Clock;
    readonly repeat?: boolean;
    readonly onFinish?: () => void;
}
```

### type `TimingState`

```ts
export type TimingState = "idle" | "playing" | "paused" | "finished" | "cancelled";
```

### type `ToggleAction`

```ts
export type ToggleAction = "play" | "pause" | "resume" | "reverse" | "reset" | "restart" | "complete" | "none";
```

### type `ToggleActions`

```ts
export type ToggleActions = readonly [
    ToggleAction,
    ToggleAction,
    ToggleAction,
    ToggleAction
];
```

### type `TrackerEvent`

```ts
export type TrackerEvent = {
    readonly type: "start";
    readonly id: number;
    readonly record: AnimationRecord;
} | {
    readonly type: "finish";
    readonly id: number;
} | {
    readonly type: "cancel";
    readonly id: number;
};
```

### type `TrackerListener`

```ts
export type TrackerListener = (event: TrackerEvent) => void;
```

### interface `TriggerGeom`

```ts
export interface TriggerGeom {
    readonly elementTop: number;
    readonly elementHeight: number;
    readonly viewportHeight: number;
}
```

### interface `TriggerPos`

```ts
export interface TriggerPos {
    readonly element: number;
    readonly viewport: number;
}
```

### interface `TweenOpts`

```ts
export interface TweenOpts {
    readonly duration?: number;
    readonly easing?: EasingFn;
}
```

### type `TweenProps`

```ts
export type TweenProps = Record<string, readonly unknown[]>;
```

### type `TweenValue`

```ts
export type TweenValue<P extends TweenProps> = {
    [K in keyof P]: P[K] extends readonly (infer V)[] ? Widen<V> : never;
};
```

### interface `UniformBinding`

```ts
export interface UniformBinding<T = unknown> {
    readonly apply: (gl: GLLike, value: T) => void;
}
```

### type `UniformBindings`

```ts
export type UniformBindings<V> = {
    readonly [K in keyof V]: UniformBinding<V[K]>;
};
```

### type `UniformLocation`

```ts
export type UniformLocation = WebGLUniformLocation | null;
```

### class `UnitMismatchError`

```ts
export declare class UnitMismatchError extends Error {
    readonly fromUnit: string;
    readonly toUnit: string;
    constructor(fromUnit: string, toUnit: string);
}
```

### interface `UnitValue`

```ts
export interface UnitValue {
    readonly value: number;
    readonly unit: string;
}
```

### type `ValueInterpolator`

```ts
export type ValueInterpolator<T> = (from: T, to: T) => (progress: number) => T;
```

### type `ValueOf`

```ts
export type ValueOf<A> = A extends AnimationDef<infer T> ? T : never;
```

### type `ValuesCommit`

```ts
export type ValuesCommit<V> = (values: V) => void;
```

### type `ValuesHandle`

```ts
export type ValuesHandle = TimingHandle;
```

### type `ValuesOpts`

```ts
export type ValuesOpts = TimingOpts;
```

### interface `VelocityOpts`

```ts
export interface VelocityOpts {
    readonly windowMs?: number;
    readonly maxSamples?: number;
}
```

### interface `VelocityTracker`

```ts
export interface VelocityTracker {
    record(sample: PointerSample): void;
    velocity(): Point;
    reset(): void;
}
```

### interface `WaapiAnimation`

```ts
export interface WaapiAnimation {
    pause(): void;
    play(): void;
    cancel(): void;
    finish(): void;
    reverse(): void;
    currentTime: unknown;
    playbackRate: number;
    readonly finished: Promise<unknown>;
    onfinish: unknown;
    oncancel: unknown;
}
```

### interface `WaapiHandle`

```ts
export interface WaapiHandle {
    pause(): void;
    resume(): void;
    seek(progress: number): void;
    reverse(): void;
    setSpeed(multiplier: number): void;
    cancel(): void;
    readonly state: WaapiState;
    readonly progress: number;
    readonly direction: 1 | -1;
    readonly finished: Promise<void>;
}
```

### interface `WaapiOpts`

```ts
export interface WaapiOpts {
    readonly sampleRateHz?: number;
    readonly minSamples?: number;
    readonly maxSamples?: number;
    readonly fill?: "forwards" | "none" | "both";
}
```

### type `WaapiState`

```ts
export type WaapiState = "idle" | "playing" | "paused" | "finished" | "cancelled";
```

### interface `WaveOpts`

```ts
export interface WaveOpts {
    readonly amplitude: number;
    readonly frequency?: number;
    readonly phase?: number;
}
```

### type `WebGLHandle`

```ts
export type WebGLHandle = ValuesHandle;
```

### type `WebGLOpts`

```ts
export type WebGLOpts = ValuesOpts;
```

### interface `WorkerAnimSpec`

```ts
export interface WorkerAnimSpec {
    readonly id: string;
    readonly startTime: number;
    readonly duration: number;
    readonly easing: WorkerEasingId;
    readonly properties: Readonly<Record<string, readonly [
        number,
        number
    ]>>;
    readonly holdAtEnd?: boolean;
}
```

### interface `WorkerComputeRequest`

```ts
export interface WorkerComputeRequest {
    readonly type: "compute";
    readonly seq: number;
    readonly time: number;
    readonly specs: readonly WorkerAnimSpec[];
}
```

### interface `WorkerComputeResponse`

```ts
export interface WorkerComputeResponse {
    readonly type: "values";
    readonly seq: number;
    readonly values: WorkerValues;
}
```

### interface `WorkerComputerOpts`

```ts
export interface WorkerComputerOpts {
    readonly mode?: ComputerMode;
    readonly Worker?: WorkerCtor | undefined;
    readonly workerUrl?: () => string;
}
```

### type `WorkerEasingId`

```ts
export type WorkerEasingId = "linear" | "ease-in" | "ease-out" | "ease-in-out" | {
    readonly kind: "cubic-bezier";
    readonly x1: number;
    readonly y1: number;
    readonly x2: number;
    readonly y2: number;
} | {
    readonly kind: "steps";
    readonly n: number;
    readonly position: "start" | "end";
};
```

### type `WorkerValues`

```ts
export type WorkerValues = Readonly<Record<string, Readonly<Record<string, number>>>>;
```

### type `WriteJob`

```ts
export type WriteJob = () => void;
```

### function `__resetTracker`

```ts
export declare function __resetTracker(): void;
```

### function `animation`

```ts
export declare function animation<T>(interpolate: (progress: number) => T, duration: number, easing?: EasingFn): AnimationDef<T>;
```

### function `applyValues`

```ts
export declare function applyValues(el: ElementShim, values: Readonly<Record<string, PropertyValue>>): void;
```

### const `batch`

```ts
declare const batch: Batch
```

### function `buildKeyframes`

```ts
export declare function buildKeyframes(def: AnimationDef<Readonly<Record<string, unknown>>>, opts?: WaapiOpts): Keyframe[];
```

### function `classify`

```ts
export declare function classify(property: string): PropertyInfo;
```

### function `computeBounds`

```ts
export declare function computeBounds(start: TriggerPos, end: TriggerPos, geom: TriggerGeom): ScrollBounds;
```

### function `computeValues`

```ts
export declare function computeValues(specs: readonly WorkerAnimSpec[], time: number): WorkerValues;
```

### function `createBatch`

```ts
export declare function createBatch(): Batch;
```

### function `createClock`

```ts
export declare function createClock(opts?: ClockOpts): Clock;
```

### function `createDomPointerSource`

```ts
export declare function createDomPointerSource(): PointerSource;
```

### function `createDomScrollSource`

```ts
export declare function createDomScrollSource(win?: MinimalWindow): ScrollSource;
```

### function `createFrameScheduler`

```ts
export declare function createFrameScheduler(opts?: FrameSchedulerOpts): FrameScheduler;
```

### function `createTiming`

```ts
export declare function createTiming<V>(def: AnimationDef<V>, commit: (values: V) => void, opts?: TimingOpts): TimingHandle;
```

### function `createVelocityTracker`

```ts
export declare function createVelocityTracker(opts?: VelocityOpts): VelocityTracker;
```

### function `createWorkerComputer`

```ts
export declare function createWorkerComputer(opts?: WorkerComputerOpts): Computer;
```

### function `cubicBezier`

```ts
export declare function cubicBezier(x1: number, y1: number, x2: number, y2: number): EasingFn;
```

### const `defaultClock`

```ts
declare const defaultClock: Clock
```

### function `delay`

```ts
export declare function delay<T>(anim: AnimationDef<T>, ms: number): AnimationDef<T>;
```

### function `detectWaapi`

```ts
export declare function detectWaapi(): boolean;
```

### function `discoverProperties`

```ts
export declare function discoverProperties(def: AnimationDef<AnimationProps>): readonly string[];
```

### const `easeIn`

```ts
declare const easeIn: EasingFn
```

### const `easeInOut`

```ts
declare const easeInOut: EasingFn
```

### const `easeOut`

```ts
declare const easeOut: EasingFn
```

### function `enableTracker`

```ts
export declare function enableTracker(): void;
```

### function `findInterpolator`

```ts
export declare function findInterpolator(value: unknown): InterpolatorEntry | null;
```

### function `float`

```ts
export declare function float(loc: UniformLocation): UniformBinding<number>;
```

### const `frame`

```ts
declare const frame: FrameScheduler
```

### function `fromGrid`

```ts
export declare function fromGrid(opts: GridOpts): StaggerFn;
```

### const `gesture`

```ts
declare const gesture: {
    drag: typeof drag;
    hover: typeof hover;
    tap: typeof tap;
    press: typeof press;
    pan: typeof pan;
    pinch: typeof pinch;
}
```

### function `getReducedMotionDefault`

```ts
export declare function getReducedMotionDefault(): ReducedMotion;
```

### function `inertia`

```ts
export declare function inertia<P extends InertiaProps>(props: P, opts?: InertiaOpts): AnimationDef<InertiaValue<P>>;
```

### function `interpolate`

```ts
export declare function interpolate<T>(from: T, to: T): (progress: number) => T;
```

### function `interpolateColor`

```ts
export declare function interpolateColor(from: string, to: string): (progress: number) => string;
```

### function `interpolateNumber`

```ts
export declare function interpolateNumber(from: number, to: number): (progress: number) => number;
```

### function `interpolatePath`

```ts
export declare function interpolatePath(from: string, to: string): (progress: number) => string;
```

### function `interpolateTransform`

```ts
export declare function interpolateTransform(from: string, to: string): (progress: number) => string;
```

### function `interpolateUnit`

```ts
export declare function interpolateUnit(from: string, to: string): (progress: number) => string;
```

### function `isColor`

```ts
export declare function isColor(value: string): boolean;
```

### function `isCompositorSafe`

```ts
export declare function isCompositorSafe(property: string): boolean;
```

### const `isSpringEasing`

```ts
declare const isSpringEasing: (fn: EasingFn) => fn is SpringEasingFn
```

### function `isTrackerEnabled`

```ts
export declare function isTrackerEnabled(): boolean;
```

### function `keyframes`

```ts
export declare function keyframes<P extends KeyframeStops>(stops: P, opts?: KeyframesOpts): AnimationDef<KeyframesValue<P>>;
```

### const `linear`

```ts
declare const linear: EasingFn
```

### function `listActiveAnimations`

```ts
export declare function listActive(): readonly AnimationRecord[];
```

### function `loop`

```ts
export declare function loop<T>(anim: AnimationDef<T>, count?: number): AnimationDef<T>;
```

### function `map`

```ts
export declare function map<A, B>(anim: AnimationDef<A>, fn: (a: A) => B): AnimationDef<B>;
```

### function `mat4`

```ts
export declare function mat4(loc: UniformLocation, transpose?: boolean): UniformBinding<readonly number[]>;
```

### function `parallel`

```ts
export declare function parallel<T extends readonly AnimationDef<unknown>[]>(...anims: T): AnimationDef<ParallelValues<T>>;
```

### function `parsePath`

```ts
export declare function parsePath(input: string): readonly PathCommand[];
```

### function `parseToggleActions`

```ts
export declare function parseToggleActions(input: string | ToggleActions): ToggleActions;
```

### function `parseTransform`

```ts
export declare function parseTransform(input: string): readonly TransformFn[];
```

### function `parseTriggerPos`

```ts
export declare function parseTriggerPos(input: string | TriggerPos): TriggerPos;
```

### function `parseUnit`

```ts
export declare function parseUnit(input: string): UnitValue | null;
```

### function `partitionByTier`

```ts
export declare function partitionByTier(props: readonly string[]): {
    readonly props: readonly string[];
    readonly compositor: readonly string[];
    readonly main: readonly string[];
};
```

### function `play`

```ts
export declare function play(def: AnimationDef<AnimationProps>, target: Target, opts?: PlayOpts): Controls;
```

### function `playDrag`

```ts
export declare function playDrag(targets: readonly StrategyTarget[], opts: DragOpts): DragHandle;
```

### function `playHover`

```ts
export declare function playHover(targets: readonly StrategyTarget[], opts: HoverOpts): HoverHandle;
```

### function `playRaf`

```ts
export declare function playRaf(def: AnimationDef<PropertyMap>, targets: readonly ElementShim[], opts?: RafOpts): RafHandle;
```

### function `playScrollSync`

```ts
export declare function playScrollSync(def: AnimationDef<AnimationProps>, targets: readonly StrategyTarget[], opts: ScrollSyncOpts): ScrollSyncHandle;
```

### function `playScrollTriggered`

```ts
export declare function playScrollTriggered(def: AnimationDef<AnimationProps>, targets: readonly StrategyTarget[], opts: ScrollTriggeredOpts): ScrollTriggeredHandle;
```

### function `playStagger`

```ts
export declare function playStagger(def: AnimationDef<AnimationProps>, target: Target, opts: PlayStaggerOpts): Controls;
```

### function `playStrategy`

```ts
export declare function playStrategy(def: AnimationDef<AnimationProps>, targets: readonly StrategyTarget[], opts?: StrategyOpts, backendOverride?: StrategyBackend): StrategyHandle;
```

### function `playUniforms`

```ts
export declare function playUniforms<V extends Record<string, unknown>>(def: AnimationDef<V>, gl: GLLike, bindings: UniformBindings<V>, opts?: WebGLOpts): WebGLHandle;
```

### function `playValues`

```ts
export declare function playValues<V>(def: AnimationDef<V>, onValue: ValuesCommit<V>, opts?: ValuesOpts): ValuesHandle;
```

### function `playWaapi`

```ts
export declare function playWaapi(def: AnimationDef<Readonly<Record<string, unknown>>>, targets: readonly Animatable[], opts?: WaapiOpts, scheduler?: FrameScheduler | null): WaapiHandle;
```

### function `prefersReducedMotion`

```ts
export declare function prefersReducedMotion(): boolean;
```

### function `progressAt`

```ts
export declare function progressAt(scrollY: number, bounds: ScrollBounds): number;
```

### function `pseudoToTransformFn`

```ts
export declare function pseudoToTransformFn(property: string): string | null;
```

### function `registerInterpolator`

```ts
export declare function registerInterpolator(entry: InterpolatorEntry, priority?: "high" | "low"): void;
```

### function `reverse`

```ts
export declare function reverse<T>(anim: AnimationDef<T>): AnimationDef<T>;
```

### function `scroll`

```ts
export declare function scroll(def: AnimationDef<AnimationProps>, target: Target, opts?: ScrollOpts): ScrollHandle;
```

### function `sequence`

```ts
export declare function sequence<T>(...anims: AnimationDef<T>[]): AnimationDef<T>;
```

### function `setReducedMotionDefault`

```ts
export declare function setReducedMotionDefault(mode: ReducedMotion): void;
```

### function `shouldReduceMotion`

```ts
export declare function shouldReduceMotion(mode?: ReducedMotion): boolean;
```

### function `shuffle`

```ts
export declare function shuffle(opts?: ShuffleOpts): StaggerFn;
```

### function `splitText`

```ts
export declare function splitText(target: HTMLElement, opts?: SplitOpts): SplitResult;
```

### function `spring`

```ts
export declare function spring<P extends TweenProps>(props: P, opts?: SpringOpts): AnimationDef<TweenValue<P>>;
```

### function `springEasing`

```ts
export declare function springEasing(opts?: SpringOpts): SpringEasingFn;
```

### function `stagger`

```ts
export declare function stagger<T>(anim: AnimationDef<T>, opts: StaggerOpts): AnimationDef<readonly T[]>;
export declare function stagger<T>(anims: readonly AnimationDef<T>[], opts: StaggerArrayOpts): AnimationDef<readonly T[]>;
```

### function `steps`

```ts
export declare function steps(n: number, position?: StepPosition): EasingFn;
```

### function `stringifyPath`

```ts
export declare function stringifyPath(cmds: readonly PathCommand[]): string;
```

### function `strokeDraw`

```ts
export declare function strokeDraw(opts: StrokeDrawOpts): AnimationDef<AnimationProps>;
```

### function `subscribeTracker`

```ts
export declare function subscribe(fn: TrackerListener): () => void;
```

### function `timeline`

```ts
export declare function timeline(): Timeline;
```

### function `trackAnimation`

```ts
export declare function trackAnimation(controls: Controls, targets: readonly StrategyTarget[], backend?: StrategyBackend): number;
```

### function `tween`

```ts
export declare function tween<P extends TweenProps>(props: P, opts?: TweenOpts): AnimationDef<TweenValue<P>>;
```

### function `vec2`

```ts
export declare function vec2(loc: UniformLocation): UniformBinding<readonly number[]>;
```

### function `vec3`

```ts
export declare function vec3(loc: UniformLocation): UniformBinding<readonly number[]>;
```

### function `vec4`

```ts
export declare function vec4(loc: UniformLocation): UniformBinding<readonly number[]>;
```

### function `wave`

```ts
export declare function wave(opts: WaveOpts): StaggerFn;
```

### function `zoneAt`

```ts
export declare function zoneAt(scrollY: number, bounds: ScrollBounds): ScrollZone;
```
